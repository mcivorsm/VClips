spring.application.name=bevc

spring.datasource.url=jdbc:postgresql://localhost:5432/vclips_user_info
spring.datasource.username=postgres
spring.datasource.password=luffyD1998!
spring.datasource.driver-class-name=org.postgresql.Driver

# Optional JPA configurations
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Disable JTA and use standard JPA transaction manager (spring-boot-starter-data-jpa configures this by default)
spring.jpa.properties.hibernate.transaction.jta.platform=org.hibernate.transaction.JDBCTransactionFactory

